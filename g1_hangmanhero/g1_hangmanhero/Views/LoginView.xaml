<Window x:Class="g1_hangmanhero.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="380" Width="350" 
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <StackPanel Margin="0,0,0,-16">
            <TextBlock Text="Hangman Game" FontSize="36" FontWeight="Bold" 
           HorizontalAlignment="Center" Margin="0,0,0,40" Foreground="Red"/>
            
            <TextBlock Text="Login" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Username:" Margin="0,0,0,0"/>
            <TextBox  Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBlock Text="Password:" Margin="0,0,0,0"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>

            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red"/>

            <Button Content="Don't have an account? Register" Click="Register_Click" HorizontalAlignment="Right" FontSize="10"/>
        </StackPanel>
    </Grid>
</Window>
